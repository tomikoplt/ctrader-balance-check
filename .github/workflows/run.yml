name: Test Telegram

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3   # <-- THIS WAS MISSING

      - name: Run script
        run: |
          pip install requests
          python script.py
